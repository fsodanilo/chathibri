# Frontend Dependencies - ChatHib
# FastAPI and related packages
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0

# Template engine
jinja2==3.1.2

# Authentication
PyJWT==2.8.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# HTTP requests
requests==2.31.0

# Security and cryptography
itsdangerous==2.1.2

# ASGI server components
starlette==0.27.0

# MongoDB support (for legacy compatibility)
pymongo==4.6.0
bson==0.5.10

# Standard library extensions (if needed)
python-dotenv==1.0.0

# File handling
python-multipart==0.0.6

# Date/time utilities (built-in, but in case of version issues)
# datetime - built-in module

# System utilities (built-in modules)
# os - built-in
# shutil - built-in
# time - built-in
# subprocess - built-in
# json - built-in
# uuid - built-in
# random - built-in
